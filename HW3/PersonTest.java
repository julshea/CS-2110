import org.junit.*;
public class PersonTest {
	//addBook() test #1:
	@Test(timeout = 100)
	public void testAddBook() {
		Arraylist<Book> b = new ArrayList<Book>;
		AssertTrue(addBook(b);)
		
}
	//numBooksRead() test #1
	@Test(timeout = 100)
	public void testNumBooksRead(){
		Arraylist<Book> b = new ArrayList<Book>;
		AssertTrue(numBooksRead == 0);
	}	
}
	
